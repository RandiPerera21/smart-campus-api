package com.smartcampus;

// No Main class needed when deploying to Tomcat via NetBeans.
// NetBeans deploys the WAR directly to Tomcat.
// Entry point is SmartCampusApplication via web.xml + @ApplicationPath.

public class Main {
    // Intentionally empty - kept only for reference.
    // To run: Right-click project in NetBeans -> Run (deploys to Tomcat automatically)
}
